---
description: Reverse-engineer a codebase into an agent-optimized knowledge base
argument-hint: <path-to-codebase>
---

Run a full codebase dissection of: $ARGUMENTS

Invoke the `dissect` skill and follow its orchestration playbook exactly, end to end.

If no path was given above, ask the user for the codebase path (or confirm the current directory) before doing anything else.
